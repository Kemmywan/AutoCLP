[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "AutoCLP"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["rag*", "core*", "config*"]
exclude = ["test*", "test_data*", "test_store*", "dialogue*", "raw*"]